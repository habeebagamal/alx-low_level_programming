#include <stdio.h>

/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
void main(void)
{
	char alpha = 'a';
        while (alpha<='z')
	{ 
		putchar(alpha);
		alpha++;
		putchar('\n');

	}
	
	
}
